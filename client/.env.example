# ─── Backend API ──────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# ─── Google OAuth (redirects to backend, not Google directly) ─────────────────
NEXT_PUBLIC_GOOGLE_AUTH_URL=http://localhost:5000/api/auth/google

# ─── GitHub OAuth (redirects to backend, not GitHub directly) ─────────────────
NEXT_PUBLIC_GITHUB_AUTH_URL=http://localhost:5000/api/auth/github
